Misinformation Detector MSc Research
> cgi-bin: contains the Python scripts that run the misinformation classification model
> data: contains the datasets used to train the model(s)
> extension: contains the HTML/CSS/JS files that run the Chrome extension